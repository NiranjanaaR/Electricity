DATABASE_URL=postgresql+psycopg2://obxai:obxai@localhost:5432/obxai
ANALYSIS_LOOKBACK_DAYS=180
SCHEDULE_HOUR=7
SCHEDULE_MINUTE=0
TIMEZONE=Europe/Oslo
ALLOW_ORIGINS=http://localhost:5173,http://localhost:3000

# Optional secondary data providers — used as fallbacks when Yahoo Finance
# is unreachable. Leave blank to disable. No synthetic data is ever used.
ALPHAVANTAGE_API_KEY=
FINNHUB_API_KEY=
